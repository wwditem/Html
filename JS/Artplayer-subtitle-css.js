art.on('ready', () => {
		    art.seek = 20;
		    setTimeout(() => {
		        art.subtitle.style({
		            color: 'FFFFFF',
		            'font-size': '50px',
					'margin-bottom': '30px',
		        });
		    }, 3000);
		});
